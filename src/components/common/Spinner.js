import React from 'react';
import { View, Text } from 'react-native';
import { WaveIndicator } from 'react-native-indicators';

const Spinner = ({size}) => {
  return <WaveIndicator color='#10E7DC' />
}

export default Spinner;
